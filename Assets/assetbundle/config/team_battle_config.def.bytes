
à
config_team_battle_config.proto
sgk.config"°
config_team_battle_config7
rows (2).sgk.config.config_team_battle_config.RowÙ
Row
gid_id (
tittle_name (	
	difficult (
difficult_name (	
activity_id (
find_npc (
use_picture (	
use_picture_small (	
limit_level	 (
des
 (	
fresh_time_des (	
mapid (
map_id (	
battle_icon (	
battle_back (	

chapter_id (
chapter_name (	
group_id (

group_name (	
battle_name (	
desc (	
sound (
	lev_limit (
team_member (
rely_battle (
	finish_id (
type1 (
drop1 (
type2 (
drop2 (
type3 (
drop3  (

begin_time! (
end_time" (
duration_per_period# (
valid_per_period$ (
script% (	
enter_x& (
enter_y' (
enter_z( (
enter_script) (	
	des_limit* (
chapter+ (
day, (
rewrad_count_one- (
rewrad_count_ten. (

limit_time/ (
final_fight0 (
story1 (	
story_title2 (	
battle_music3 (	