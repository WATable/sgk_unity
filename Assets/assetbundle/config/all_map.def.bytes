
ı
config_all_map.proto
sgk.config"–
config_all_map,
rows (2.sgk.config.config_all_map.Rowè
Row
gid (
map_id (	
map_name (	
map_type (
map_move_style (
map_authority (
depend_level (
depend_quest_id (
camera	 (	
click_texiao
 (	
button (
	sceneback (
script (	

from_mapid (	
to_mapid (	
isopen_allmap (
use_icon (	
title (	
from_pos (
to_pos (
initialposition_x (
initialposition_y (
initialposition_z (
chat (
mapImage (	
Uishow (
Uiquest (	