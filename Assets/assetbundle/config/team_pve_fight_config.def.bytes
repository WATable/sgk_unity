
Á	
"config_team_pve_fight_config.proto
sgk.config"Ž	
config_team_pve_fight_config:
rows (2,.sgk.config.config_team_pve_fight_config.Row±
Row
gid (
gid_id (
sequence (
is_fight_npc (
depend_level_id (
depend_fight0_id (
depend_fight1_id (
scene_bg_id (	

scene_name	 (	
npc_id
 (
npc_name (	

monster_id (
scale (
count_per_day (
rank (
support_god_hand (
cost_item_type (
cost_item_id (
cost_item_value (
can_yjdq (
rely_fight_id (
duration (
exp (
win_type (
win_para (

star1_type (

star1_para (

star2_type (

star2_para (

star3_type (

star3_para (
music  (	

boss_music! (	
capacity" (
public_drop# (
drop1$ (
drop2% (
drop3& (

pick_limit' (
effect_type( (
effect_value) (

effect_who* (
effect_battle_id+ (
effect_fight_id, (
win_des- (	
story_id. (
after_fight_story/ (
born_effect0 (	

fight_type1 (

score_type2 (
	key_value3 (	
fight_group4 ( 
pubilc_drop_consume_item5 (